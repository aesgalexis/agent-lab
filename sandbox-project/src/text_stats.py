def word_count(text: str) -> int:
    """Return the number of whitespace-separated words in *text*."""
    # Intentional phase-1 defect: the agent should repair this implementation.
    return len(text)
